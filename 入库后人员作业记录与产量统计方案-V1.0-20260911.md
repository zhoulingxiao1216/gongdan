# 入库后人员作业记录与产量统计方案

版本：V1.0 评审稿  
日期：2026-09-11  
业务范围：代购商品附加项处理后的入库、建单、配货、打包、清算、发货  
文档定位：产品方案与实施范围建议，供业务、产品、研发评审；不代表已批准的开发需求。

## 1. 方案结论

推荐建设“统一人员作业台账与产量统计”，复用现有 PDA 作业记录和附加项报工数据。

入库后的作业关联当前业务单据：入库关联入库明细，配货关联配货任务或发货商品明细，打包关联包裹或箱。人员产量统计不以关联代购订单为前提。

现有 PDA 已能够通过登录账号区分谁配了哪个发货单的什么商品，因此配货人员追踪能力直接复用。是否能够直接用于数量统计，仍需核实实际数量、时间、撤销等字段和记录口径。

只有业务进一步要求“从某个代购订单查看入库后的完整履约情况”，才增加库存来源追溯，不将其作为本次人员统计的前置条件。

## 2. 现状、依据与边界

### 2.1 已确认事实

| 编号 | 已确认内容 | 方案影响 |
|---|---|---|
| C01 | 附加项处理后，业务依次包括商品入库、创建发货单、配货、打包、清算、发货 | 补充入库后人员作业记录的整体设计 |
| C02 | 商品入库后已不再关联代购订单 | 后续人员记录使用当前作业对象，不强制关联原订单 |
| C03 | 入库、配货、打包均可以在 PDA 操作 | 优先复用当前操作入口，不增加重复报工入口 |
| C04 | PDA 通过登录账号区分谁配了哪个发货单的什么商品 | 配货已有人员和业务对象关联基础，无需重建该能力 |
| C05 | 现有附加项 PRD 将人员报工与清算分开，不以未报工新增入库、发货拦截 | 新方案延续该原则 |

依据：本轮业务沟通；[附加项报工小程序 PRD](附加项报工小程序PRD-V1.0-20260910.md)中的 V1.1 修订说明、U7、业务边界与核心术语。

### 2.2 尚未核实的能力

- 配货记录是否包含本次实际数量、实际完成时间、记录唯一编号和有效状态。
- 配货是否支持分批、多人处理；撤销、更正、取消和重新配货如何保存。
- 入库与打包是否具备与配货同等完整的人员及数量记录。
- PDA 登录账号是否一人一号，是否存在共用账号、代操作和账号历史变更。
- PDA、附加项小程序及后台能否映射到同一员工编号。
- 原系统可通过接口、事件或数据库视图中的哪种方式提供数据。
- 创建发货单、清算、发货的现有操作入口与人员记录能力。

以上事项不得在开发评审前直接认定为已具备。本方案没有审计生产系统代码、数据库或接口。

### 2.3 本次不改变的业务规则

清算继续使用现有业务口径。人员台账不回写清算数量、金额或业务完成状态，不因未记录报工新增业务流转拦截。原系统已有业务校验继续有效。

本次讨论的是扩展方案，不自动修改既有附加项首版 PRD 的交付范围。

## 3. 业务目标

1. 管理人员能在一个入口查询员工完成了哪些附加项、入库、配货和打包工作。
2. 员工继续使用现有作业入口，已有可靠记录不要求再次手工报工。
3. 按员工、作业环节、日期分别统计有效产量，并可下钻到来源记录。
4. 区分业务状态、人员实际工作量和订单来源追溯，避免混用。

本阶段不制定工资、绩效排名、标准工时、作业单价或订单成本分摊规则。

## 4. 方案比较

| 方案 | 建设内容 | 优点 | 局限 | 相对投入 |
|---|---|---|---|---|
| 一：保留各环节查询 | 附加项查报工记录，仓内作业查 PDA 或原业务后台 | 改动少，可继续使用现有能力 | 跨系统查询繁琐，难以形成统一人员视图 | 低 |
| 二：统一人员作业台账，推荐 | 汇集附加项和 PDA 有效记录，统一员工身份、查询和分类统计 | 解决人员工作量问题，不依赖代购订单关联 | 需统一字段、有效性和更正规则；不能直接实现订单全链路追溯 | 中，取决于现有数据质量 |
| 三：统一台账加订单来源追溯 | 在方案二上增加入库来源、库存分配与发货商品的数量关联 | 可进一步支持代购订单履约追溯和成本分析 | 涉及拆分、合并、调拨、退回及历史断链，改造范围大 | 高 |

推荐先实施方案二。若业务只需零散查人查单，方案一也可满足；只有订单来源追溯成为明确需求时，再评审方案三。

独立派工任务、单件码或容器码追踪属于后续能力。当前已具备 PDA 作业入口，不建议为统一统计而强制重建任务系统或重新贴码。

## 5. 业务流程与各环节记录口径

```text
附加项实体处理 → 商品入库 → 创建发货单 → 配货 → 打包 → 清算 → 发货
      ↓             ↓           ↓          ↓      ↓       ↓      ↓
 附加项报工      现有PDA记录   建单记录    PDA记录 PDA记录 处理记录 交接记录
      └───────────────────────┬───────────────────────────────┘
                              ↓
                 统一员工身份、作业分类、有效性口径
                              ↓
                    人员作业台账与分类产量统计
```

图中除已确认的配货人员追踪外，其他来源记录的字段完整性和可接入性均需核实。作业记录缺失不反向改变既有业务流转规则。

| 环节 | 建议关联对象 | 建议统计单位 | 有效作业依据 | 本次建议 |
|---|---|---|---|---|
| 附加项 | 代购商品明细＋具体附加项实例 | 件；跨项目汇总称作业件次 | 现有有效报工记录 | 复用已有口径 |
| 入库 | 入库明细或上架任务明细 | 件，单数辅助 | 实际入库或上架确认成功 | 核实人员、数量及动作含义后接入 |
| 创建发货单 | 发货单 | 人工有效建单数 | 有效人工建单操作 | 辅助统计，自动建单不算人员产量 |
| 配货 | 配货任务明细或发货商品明细 | 件，明细行数辅助 | 本次实际配货确认成功 | 优先复用现有 PDA 记录 |
| 打包 | 包裹或箱＋所属发货单 | 包裹或箱，商品件数辅助 | 实际打包、封箱确认成功 | 核实记录完整性后接入 |
| 清算 | 清算处理记录＋相关单据 | 人工处理单数 | 真实人工处理操作 | 辅助统计；自动清算不算人员产量 |
| 发货 | 实际交接明细＋包裹或箱 | 包裹或箱 | 实物出库交接确认 | 核实入口；不能只凭物流状态推算人员工作 |

入库确认如仅为系统库存转换，不能直接认定为人工上架产量。收货与上架如果是两项独立工作，应分设作业类型。

同一商品经过配货和打包可以分别计入对应环节，但件数与箱数不可相加为统一“总产量”。包裹与箱是否同义应按现行业务确定，不默认互换。

## 6. 推荐产品设计

### 6.1 入口分工

| 入口 | 职责 |
|---|---|
| PDA | 延续入库、配货、打包操作；复用已有记录，仅补齐确认缺失的必要字段 |
| 附加项小程序 | 延续附加项报工；个人跨环节产量查询可作为后续增强 |
| PC 后台 | 提供统一人员作业台账、分类汇总、来源下钻和数据异常查看 |
| 原业务系统 | 继续负责业务单据、库存、清算及其状态管理 |

### 6.2 PC 人员作业台账

筛选条件：作业日期、员工、作业环节、业务单号、商品、数据来源、记录状态。

列表建议字段：作业时间、员工、作业环节、业务单号、商品或包裹、本次数量、单位、有效状态、数据来源。

详情展示来源记录编号、实际处理人、提交人、同步时间、更正或冲销关系，并支持进入原业务单据。无代购订单关联时展示当前业务对象，不要求补填代购订单。

### 6.3 人员产量汇总

基础维度为“员工 × 实际作业日期 × 作业类型 × 计量单位”，点击数量可下钻有效明细。

例如同一员工当日可以展示：贴纸 80 件、配货 120 件、打包 8 箱。不得展示为 208 件总产量。不同附加项相加时标注作业件次，避免被理解为商品实物数量。

界面应显示数据更新时间；同步未完成时提示数据延迟，不能将缺失记录显示成确定的零产量。

## 7. 关键规则

### 7.1 员工身份

使用稳定员工编号关联 PDA 和附加项账号，不按姓名合并。保留来源账号以便追查。

登录人作为实际处理人的前提是账号确实由本人使用。存在代操作时，分别保存实际处理人和提交人；历史无法确认的记录标记待匹配，不直接分配给推测的员工。

### 7.2 数量与分批处理

记录本次成功完成量，不将单据计划数量或最终状态直接当作产量。

例如配货 100 件，甲完成 60 件、乙完成 40 件，记录两笔。整单完成时不再追加 100 件。若原系统仅提供累计量，必须先制定可靠的增量识别规则，不能把每次累计快照相加。

### 7.3 去重与数据一致性

同一来源作业明细重复提交、网络重试或重复同步只能计量一次。建议使用来源系统、来源记录编号及必要的明细编号建立唯一对应关系，更正版本更新不得生成重复产量。

来源业务确认失败不计量。同步失败应重试、告警并支持对账补齐，不要求员工重复完成作业。

### 7.4 更正、撤销与返工

| 场景 | 统计处理 |
|---|---|
| 数量录错或重复记录 | 更正或冲销对应记录，保留历史关系 |
| 配货实际完成后客户取消发货 | 保留已发生的配货工作，不因单据取消自动清零 |
| 已配商品退回上架 | 按实际退回上架作业另行记录 |
| 重新配货或重新打包 | 单列返工作业，避免混入首次正常作业产量 |
| 补传延迟记录 | 按实际作业日期归属，并保留补传时间 |

更正入口优先复用来源系统。统一台账原则上不直接改写来源业务记录；若来源系统没有更正机制，需要另行评审统计调整流程和操作权限。

### 7.5 多人协作

能按实际数量拆分的作业，分别记录个人完成量。共同完成一个包裹时，业务总量仍为一个包裹；可保留参与人，个人贡献分摊规则未确认前，不给每人各计一个完整包裹并相加。

### 7.6 清算与人员产量独立

自动清算、自动建单、状态自动流转不产生人工产量。PDA 自动采集人员记录，必须有真实作业行为、可确认的人员和实际数量作为依据。

## 8. 数据接入建议

### 8.1 最小字段

| 字段 | 用途 |
|---|---|
| 来源系统、来源记录及明细编号 | 去重、对账、来源下钻 |
| 作业类型 | 区分附加项、入库、配货、打包等 |
| 业务对象类型、单据及明细编号 | 关联当前作业对象 |
| 商品标识或包裹/箱标识 | 根据作业对象记录，商品 SKU 不替代明细身份 |
| 员工编号、来源账号、提交人 | 产量归属和操作追查 |
| 本次数量、计量单位 | 分类汇总 |
| 实际作业时间、来源更新时间、同步时间 | 日期归属、增量同步和延迟判断 |
| 有效状态、更正或冲销关联 | 正确处理变化和历史 |
| 正常作业/返工标记 | 区分作业性质 |

实际处理人、数量或作业含义不明的记录，进入待核实清单，不直接作为已确认人员产量。

### 8.2 接入方式选择

优先使用原系统已有稳定接口或事件。若系统规模和实时性要求允许，可采用定时增量同步到统计明细表，并设置对账补偿。具体方式由研发根据现有系统能力确定，本方案不预设已有接口。

需要同时同步新增记录和历史更正，不能只按创建时间读取新增数据。上线前确认历史回补范围、同步频率、延迟目标和对账责任人。

原系统继续作为作业事实来源，统一台账作为查询统计层，避免两处分别维护同一笔作业事实。

## 9. 代购订单追溯的后续扩展

如果后续需要按代购订单追溯，可增加以下关系：

```text
代购商品明细 → 入库来源记录 → 库存批次
                                  ↓
                         实际配货分配记录
                                  ↓
                         发货商品明细 → 包裹
```

每个关系需要保存对应数量，支持一个来源拆成多次发货、多个来源合并发货。仅在发货单增加一个代购订单号无法表达这些关系。

来源商品已混放且历史关系缺失时，不能可靠还原实物来源。按规则计算的分摊只能标为管理分摊，不能展示为真实实物追溯。

## 10. 实施分期与交付物

| 阶段 | 工作内容 | 交付与完成条件 |
|---|---|---|
| 数据核实 | 检查 PDA 样本、账号映射、数量和更正规则 | 字段映射表、统计口径表、数据缺口清单 |
| 首批试点 | 接入附加项与已有配货记录，建设 PC 台账和分类汇总 | 能按员工下钻，与来源样本对账一致，无重复计量 |
| 扩展接入 | 接入字段完整的入库、打包记录；补齐确认存在的数据缺口 | 对应环节通过数量、人员和更正验收 |
| 按需增强 | 发货记录、建单和清算辅助统计、个人查询 | 根据业务价值单独确认范围 |
| 独立后续项目 | 代购订单全链路来源追溯 | 需求明确后专项评审 |

分期不代表入库和打包必须延期。若核实后已具备完整有效记录，可与首批同时接入。暂不估算工期，待数据接口和缺口确认后由研发评估。

## 11. 验收场景

| 场景 | 预期结果 |
|---|---|
| 配货记录不关联代购订单 | 仍可按员工、发货单和商品查询统计 |
| 甲配 60 件、乙配 40 件 | 分别归属两人，总配货量 100 件 |
| 同一配货记录重复同步 | 仅计量一次 |
| 整单完成状态再次同步 | 不追加整单数量 |
| 来源确认失败 | 不产生有效产量 |
| 数量由误录 10 件更正为 8 件 | 有效汇总为 8 件，保留更正依据 |
| 实际配货后客户取消订单 | 已发生工作保留，退回作业单独记录 |
| 配货 100 件、打包 5 箱 | 分单位展示，不相加为 105 |
| PDA 和小程序为同一员工的不同账号 | 归入同一员工，各环节分别统计 |
| 历史账号无法匹配员工 | 标记待匹配，不归属猜测人员 |
| 延迟同步前一天的作业 | 归属实际作业日，显示同步时间 |
| 自动建单或自动清算 | 不产生人工产量 |
| 报工统计同步异常 | 不新增业务拦截；可补齐并对账 |
| 入库或打包来源缺少实际数量 | 标记数据缺口，不用计划量替代实际量 |

## 12. 评审待确认事项

| 编号 | 待确认问题 | 建议参与方 |
|---|---|---|
| Q01 | 本期目标是否仅为人员作业查询和产量统计，暂不做代购订单全链路追溯？ | 业务负责人、产品 |
| Q02 | 配货记录的实际数量、时间、唯一编号、撤销和更正字段是否完整？ | PDA 研发、仓库主管 |
| Q03 | 入库操作代表收货、库存转换还是实际上架，是否需要拆分作业类型？ | 仓库主管、产品 |
| Q04 | 打包以包裹还是箱计量，多人共同打包如何记录？ | 仓库主管、产品 |
| Q05 | 是否存在共用账号或代操作，统一员工编号如何维护？ | 系统管理员、业务负责人 |
| Q06 | 数据如何接入，历史回补范围、允许延迟和对账责任如何确定？ | 研发、产品、运营 |
| Q07 | 返工与统计更正是否已有来源记录机制？ | 仓库主管、研发 |
| Q08 | 发货、人工建单和人工清算统计是否纳入本批？ | 业务负责人、产品 |

建议评审优先确定 Q01—Q05，明确目标与统计可信度，再安排接口开发和页面建设。
