# 入库后人员作业记录与产量统计方案

版本：V1.1 PDA 代码对齐优化稿  
日期：2026-09-11  
关联 PDA 项目：`D:\wwwroot\Global\global-wms`  
原方案：`入库后人员作业记录与产量统计方案-V1.0-20260911.md`  
业务范围：附加项处理后的入库、库位箱装箱/入库、创建发货单、配货、发货打包装箱、运单处理、发货确认，以及附加项人员报工的统一查询统计。  
文档定位：在 V1.0 方案基础上，结合 `global-wms` PDA 前端代码核实入口、接口和字段，形成产品方案、数据接入建议和研发核查清单。本文不代表后端表结构已审计完成，也不替代接口开发设计。

## 0. 本版优化结论

1. 继续推荐建设“统一人员作业台账与产量统计”，但事实来源应放在后端业务成功记录或事件日志，不在 PDA 前端另做一套手工报工。
2. PDA 前端已确认存在待入库、扫码入库、库位箱装箱/入库、配货、打包、运单填写和发货确认等操作入口；V1.0 中“PDA 是否具备入口”的不确定性可以下调，重点改为核实后端是否已保存稳定人员、数量、记录编号和更正状态。
3. 人员归属不能只依赖 `pack_name`、`distribution_name`、`admin_name` 等姓名文本。PDA 请求统一携带 `Authorization` token，后端应在每笔成功业务动作中固化登录 `admin_id/admin_uuid`，并在支持代操作时额外保存实际处理人。
4. 配货和打包要拆开统计：配货以本次成功配货数量或对象数为主，打包以 `boxDone` 中本次装入箱子的商品/包裹/箱为主；`distributeDone`、`completeBox` 这类“完成状态”只作为状态节点，不重复追加产量。
5. 入库、配货、打包、发货存在商品、包裹、箱三类对象，统计单位必须分开展示。商品件数、包裹数、箱数、人工操作单数不能简单相加为一个总产量。
6. 本期仍不把“代购订单全链路来源追溯”作为人员统计前置条件。当前统计绑定当前业务对象，例如入库明细、库位、发货单、发货商品明细、发货箱；如后续要从代购订单看完整履约，再单独建设来源追溯。

## 1. PDA 项目核实摘要

### 1.1 核实范围

本次只核实 `global-wms` 前端代码、路由、接口封装和提交参数，未直接审计后端数据库、业务表、生产日志和接口返回的完整字段。因此可以确认“PDA 前端如何发起动作”，不能直接确认“后端是否已有可用于统计的永久作业流水”。

| 模块 | 代码位置 | 已核实内容 |
|---|---|---|
| 登录身份 | `api/common/login.js`、`stores/modules/user.js`、`utils/request.js` | 登录接口为 `/admin/login/login`，返回 `login_token` 和 `admin`；请求拦截器将 token 放入 `Authorization` |
| 待入库列表 | `pages/guide/storageGoods/index.vue`、`api/guide/waitStockList.js` | 读取 `/admin_pda/kuStockInLog/waitStockList`，支持客户、业务员、关键字等筛选 |
| 扫码入库 | `pages/guide/scanInbound/index.vue`、`api/guide/inboundGuide.js` | 调用 `/admin_pda/kuStockInLog/putInStock`；商品传 `order_detail_id + quantity`，包裹/箱传对应 id 列表 |
| 入库箱指引 | `pages/guide/inboundGuide/*` | 通过扫码获取物品、库位和箱信息，引导进入扫码入库或箱操作 |
| 库位箱装箱/入库 | `pages/guide/boxOperation/index.vue`、`api/guide/kuBoxInfo.js` | 装箱调用 `/admin_pda/kuBoxInfo/inbox`，入库调用 `/admin_pda/kuStockInLog/putInStock` |
| 配货 | `pages/packShip/detail/index.vue`、`api/packShip/detail.js` | 商品配货调用 `/admin_pda/shipOrderDistribute/distribute`，支持商品数量、包裹、箱；另有一键配货和配货完成 |
| 发货打包装箱 | `pages/packShip/packDetail/index.vue`、`pages/packShip/detail/singlePacking/index.vue`、`pages/packShip/detail/shipList/packing/index.vue` | 装箱调用 `/admin_pda/shipOrderPack/boxDone`，参数含 `ship_order_detail_id/store_min_box_id/store_box_id`、`box_info.quantity`、箱规、配货/打包人员姓名 |
| 发货箱管理 | `pages/packShip/detail/shipList/index.vue`、`api/packShip/shipList.js` | 支持填写箱规、清空箱子、破箱、移出数量 |
| 运单与发货 | `pages/packShip/deliveryDetail/index.vue`、`api/packShip/detail.js` | 填写/清除运单调用 `/admin_pda/shipOrderShip/updateWaybillNo`；确认发货调用 `/admin_pda/shipOrderShip/sureShip` |

### 1.2 已可作为方案依据的事实

| 编号 | 事实 | 对方案的影响 |
|---|---|---|
| C01 | PDA 已按业务域拆分为“操作指引”和“配货打包”两大现场入口 | 统一统计不需要新增一套 PDA 报工入口，应复用既有动作 |
| C02 | PDA 请求携带登录 token，用户信息中至少有 `admin` 和姓名 | 后端有条件按提交账号落人员字段，但必须确认是否已落库 |
| C03 | 入库支持商品、包裹、箱三类对象，商品入库存在本次数量 `quantity` | 入库产量必须按对象类型区分，商品件数不能由包裹/箱直接推算 |
| C04 | 配货支持商品数量、包裹、箱，也支持一键配货和配货完成 | 统计应取“本次成功配货动作”，不取完成状态；一键配货需单独确认是否算人工产量 |
| C05 | 打包装箱接口 `boxDone` 包含本次装箱数量和箱号/箱规信息 | 打包产量优先从 `boxDone` 的明细记录接入，不从整单打包完成重复计量 |
| C06 | 发货箱页面中的 `distribution_name`、`pack_name` 是可编辑姓名文本，且默认当前登录用户姓名 | 可作为展示快照，不能作为唯一员工身份 |
| C07 | PDA 已有移出、清空、破箱、清除运单等逆向或更正动作 | 统计台账必须支持冲销、更正、返工，不可只同步新增成功记录 |

### 1.3 必须继续后端核实的事项

| 编号 | 后端核实点 | 未确认前的处理 |
|---|---|---|
| B01 | 各成功接口是否生成唯一业务日志 id 或明细 id | 没有唯一 id 时，不进入正式统计；先补充事件表或日志表 |
| B02 | 成功动作是否保存提交账号 `admin_id/admin_uuid`、实际处理人、提交时间 | 只看到姓名文本或最终状态时，先列为数据缺口 |
| B03 | 配货、装箱、入库是否保存本次增量数量，而不是只保存累计数量 | 只有累计快照时，需设计增量识别规则，不能直接相加 |
| B04 | 取消、清空、移出、破箱、重新装箱、客户取消发货如何记录 | 没有更正规则时，先不做工资/绩效类用途 |
| B05 | `distributeAll` 是否代表一线人员真实逐件配货，还是系统批量完成 | 未确认前仅统计为“一键配货操作”，不按全部商品件数归属个人 |
| B06 | 包裹、箱内商品件数能否在动作发生时可靠快照 | 不能可靠快照时，包裹/箱只按对象数统计，不反推件数 |
| B07 | PDA 账号是否一人一号，是否存在共用账号或代操作 | 未建立员工映射前，只展示来源账号，不强行归并员工 |

## 2. 相比 V1.0 的关键调整

### 2.1 “不关联代购订单”调整为“统计不依赖代购订单”

V1.0 表述“商品入库后已不再关联代购订单”容易被理解为后续完全没有上游明细字段。结合 PDA 代码，应调整为：

- 人员产量统计不以代购订单号为必填前置。
- 入库商品动作仍使用 `order_detail_id`；配货/打包动作使用 `ship_order_no`、`ship_order_detail_id`、`store_min_box_id`、`store_box_id` 等当前业务对象。
- 统一台账展示当前业务对象即可；只有做代购订单履约追溯时，才继续追溯原代购来源。

### 2.2 “已有人员追踪能力”调整为“已有登录与姓名线索，需后端固化稳定身份”

PDA 前端默认带出当前登录用户姓名，并在打包相关页面允许编辑 `distribution_name`、`pack_name`。这说明现场交互已有人员线索，但统计口径不能只按姓名合并。正式方案应要求每笔作业保存：

- 提交账号：从 token 解析出的 `admin_id/admin_uuid/account/name`。
- 实际处理人：默认提交账号；如允许代操作或扫工牌，则保存被选择的员工编号。
- 姓名快照：仅用于展示和历史可读，不作为主键。
- 来源账号：保留 PDA、附加项小程序、PC 后台各自账号，统一映射到员工编号。

### 2.3 “作业完成”调整为“成功业务动作 + 有效状态”

PDA 中存在多种完成类按钮：

- `distribute`：本次配货动作。
- `distributeAll`：一键配货全部待配商品。
- `distributeDone`：配货完成状态。
- `boxDone`：保存/完成装箱动作，包含本次数量。
- `completeBox`：整单打包完成状态。
- `sureShip`：确认发货状态。

统计人员产量时，优先取含本次对象和数量的动作。完成状态只用于判断流程节点，不重复制造产量。

### 2.4 “打包”拆为两个统计层级

发货打包至少有两个可统计动作：

| 动作 | 建议口径 | 说明 |
|---|---|---|
| 商品/包裹/箱装入发货箱 | 装箱件数、包裹数、箱数 | 来源 `boxDone`，有本次数量或对象 |
| 发货箱信息填写/完成 | 箱数、人工操作次数 | 适合统计包装完成或箱规填写，不可再重复统计商品件数 |

如果业务只关心“谁打了几个箱”，用箱数；如果关心“谁把多少商品装进箱”，用 `boxDone` 明细件数。两者可以并列，不可相加为同一个总量。

## 3. 推荐业务口径

```text
附加项实体处理
  → 商品/包裹/箱入库
  → 创建发货单
  → 配货
  → 发货打包装箱
  → 运单填写/清算
  → 发货确认
        ↓
后端成功业务动作 / 作业事件 / 更正事件
        ↓
统一员工身份映射、有效性规则、对象类型和计量单位
        ↓
人员作业台账、分类产量汇总、异常数据清单
```

### 3.1 各环节统计口径

| 环节 | PDA/系统动作 | 建议主统计单位 | 有效依据 | 不计入或单列 |
|---|---|---|---|---|
| 附加项 | 附加项小程序或后续 PDA 报工 | 作业件次 | 有效成功报工记录 | 设计预览、打印、清算默认完成 |
| 商品入库 | `putInStock`，含 `order_detail_id + quantity` | 件 | 入库成功且有本次数量 | 只展示在前端“本次已入库记录”的会话数据 |
| 包裹入库 | `putInStock`，含 `store_min_box_ids` | 包裹数；商品件数为辅助 | 入库成功且有包裹明细快照 | 无明细快照时不反推件数 |
| 箱入库 | `putInStock`，含 `store_box_ids` | 箱数；商品件数为辅助 | 入库成功且有箱明细快照 | 只凭箱状态变化不直接算人工产量 |
| 库位箱装箱 | `kuBoxInfo/inbox` | 入箱件数/包裹数 | 装箱成功，数量为 `order_detail_qty` 或对象数 | 清空、移出作为更正或返工 |
| 创建发货单 | 原业务系统建单 | 人工建单数 | 有人工创建记录 | 自动建单不算人员产量 |
| 商品配货 | `shipOrderDistribute/distribute`，含 `goods_qty + ship_order_detail_id` | 件 | 配货成功且有本次数量 | `distributeDone` 不追加数量 |
| 包裹/箱配货 | `shipOrderDistribute/distribute`，含包裹/箱 id | 包裹数/箱数；商品件数辅助 | 配货成功且有对象明细 | 无明细快照时不反推件数 |
| 一键配货 | `shipOrderDistribute/distributeAll` | 单列“一键配货操作” | 业务确认其代表真实人工逐件操作后，才可展开件数 | 默认不按全部商品件数归属个人 |
| 发货装箱 | `shipOrderPack/boxDone` | 件、包裹数、箱数 | 装箱成功，取 `box_info.quantity` 或对象数 | `completeBox` 不重复统计商品件数 |
| 发货箱完成 | `writeBoxInfo`、`completeBox` | 箱数或完成单数 | 业务确认该动作代表打包完成 | 只填箱规但非实物打包时应单列 |
| 运单填写 | `shipOrderShip/updateWaybillNo` | 箱数或操作次数 | 人工填写/修改成功 | 清除运单作为更正，不算正向产量 |
| 发货确认 | `shipOrderShip/sureShip` | 发货单数；箱数辅助 | 业务确认有实物交接动作 | 物流状态自动变化不算人员产量 |
| 清算 | 原业务系统清算 | 人工处理单数 | 真实人工清算处理记录 | 自动清算、默认完成不算人员产量 |

### 3.2 作业类型建议

| 作业类型 | 来源 | 业务对象 | 主单位 |
|---|---|---|---|
| `VAS_WORK` | 附加项报工 | 商品明细 + 附加项任务 | 作业件次 |
| `STOCK_IN_GOODS` | `putInStock` | 入库商品明细 | 件 |
| `STOCK_IN_PACKAGE` | `putInStock` | 包裹 | 包裹 |
| `STOCK_IN_BOX` | `putInStock` | 库位箱/大箱 | 箱 |
| `WAREHOUSE_BOX_INBOX_GOODS` | `kuBoxInfo/inbox` | 库位箱 + 商品 | 件 |
| `WAREHOUSE_BOX_INBOX_PACKAGE` | `kuBoxInfo/inbox` | 库位箱 + 包裹 | 包裹 |
| `SHIP_DISTRIBUTE_GOODS` | `shipOrderDistribute/distribute` | 发货单明细 | 件 |
| `SHIP_DISTRIBUTE_PACKAGE` | `shipOrderDistribute/distribute` | 发货单 + 包裹 | 包裹 |
| `SHIP_DISTRIBUTE_BOX` | `shipOrderDistribute/distribute` | 发货单 + 箱 | 箱 |
| `SHIP_PACK_GOODS` | `shipOrderPack/boxDone` | 发货箱 + 发货商品明细 | 件 |
| `SHIP_PACK_PACKAGE` | `shipOrderPack/boxDone` | 发货箱 + 包裹 | 包裹 |
| `SHIP_PACK_BOX` | `shipOrderPack/boxDone` | 发货箱 + 被装入箱 | 箱 |
| `SHIP_BOX_FINISH` | `writeBoxInfo` 或 `completeBox` | 发货箱/发货单 | 箱/单 |
| `SHIP_WAYBILL_FILL` | `updateWaybillNo` | 发货箱 | 箱/次 |
| `SHIP_CONFIRM` | `sureShip` | 发货单 | 单 |
| `SETTLEMENT_MANUAL` | 原系统清算 | 清算单/发货单 | 单 |

## 4. 数据设计建议

### 4.1 统一作业事实表或统计明细视图

建议建设统一作业事实表，也可以先用“来源业务表 + 统计视图”实现；但无论技术选型如何，统计层至少需要以下字段：

| 字段 | 说明 |
|---|---|
| `source_system` | `pda_global_wms`、`vas_miniapp`、`admin_pc` 等 |
| `source_endpoint/source_action` | 来源接口或动作枚举，便于排查 |
| `source_record_id` | 来源成功业务记录 id，去重主键之一 |
| `source_detail_id` | 来源明细 id，例如发货商品明细、箱内商品明细 |
| `work_type` | 统一作业类型 |
| `biz_object_type` | 商品、包裹、库位箱、发货箱、发货单、清算单 |
| `biz_object_id/biz_object_no` | 当前业务对象 id 和可读编号 |
| `parent_object_id/parent_object_no` | 所属发货单、箱、库位等 |
| `goods_id/sku_id/order_detail_id/ship_order_detail_id` | 商品识别字段，按来源保留 |
| `employee_id` | 统一员工编号，正式统计主键 |
| `source_admin_id/source_admin_uuid/source_account` | 来源账号，保留追查 |
| `operator_name_snapshot` | 当时姓名快照，仅展示 |
| `submit_admin_id` | 提交账号；代操作时与 `employee_id` 可不同 |
| `quantity` | 本次有效数量 |
| `unit` | 件、包裹、箱、单、次、作业件次 |
| `occurred_at` | 实际作业时间 |
| `source_created_at/source_updated_at` | 来源记录创建、更新时间 |
| `synced_at` | 进入统一台账时间 |
| `status` | 有效、已冲销、已更正、待匹配、待核实 |
| `correction_of` | 更正或冲销的原记录 |
| `is_rework` | 是否返工 |
| `extra_snapshot` | 作业发生时的数量、箱内明细、库位、备注等快照 |

### 4.2 员工映射

统一统计使用稳定员工编号，不按姓名合并。建议维护员工映射表：

| 字段 | 说明 |
|---|---|
| `employee_id` | 统一员工编号 |
| `employee_name` | 当前姓名 |
| `source_system` | PDA、小程序、PC 等 |
| `source_admin_id/source_admin_uuid/account` | 各系统账号 |
| `valid_from/valid_to` | 映射有效期 |
| `status` | 启用、停用、待确认 |

历史记录按作业发生时的账号映射归属；无法确认的记录进入待匹配清单，不用相同姓名猜测合并。

### 4.3 唯一性和幂等

建议唯一键按来源系统和来源业务记录建立：

```text
source_system + source_action + source_record_id + source_detail_id + correction_version
```

如果现有接口只更新最终状态、没有明细 id，应优先补后端事件记录。不要用前端时间、商品 id、姓名、数量拼接成统计唯一键，这类规则会在重复扫码、分批作业、多人协作时误判。

## 5. 数据接入方式

### 5.1 优先方案：后端成功动作同时写作业事件

PDA 前端已携带 token，最低风险的接入方式是在后端业务接口成功提交事务内写入作业事件：

| 来源接口 | 建议写入事件 | 关键字段 |
|---|---|---|
| `/admin_pda/kuStockInLog/putInStock` | 入库事件 | 对象类型、对象 id、库位 id、商品数量、提交账号、客户/库位快照 |
| `/admin_pda/kuBoxInfo/inbox` | 库位箱装箱事件 | 箱 id、商品/包裹 id、本次数量、提交账号 |
| `/admin_pda/shipOrderDistribute/distribute` | 配货事件 | 发货单、发货明细/包裹/箱、本次数量、提交账号 |
| `/admin_pda/shipOrderDistribute/distributeAll` | 一键配货事件 | 发货单、展开明细或单列操作、提交账号、业务确认标记 |
| `/admin_pda/shipOrderPack/boxDone` | 发货装箱事件 | 发货单、发货箱、装入对象、本次数量、提交账号、实际打包人 |
| `/admin_pda/shipOrderPack/writeBoxInfo` | 发货箱完成/箱规事件 | 发货箱、箱规、提交账号、实际打包人 |
| `/admin_pda/shipOrderPack/clearBox`、`removeQuantity`、`shipBoxUnbox` | 更正/移出/返工事件 | 原箱、对象、数量、提交账号、原因 |
| `/admin_pda/shipOrderShip/updateWaybillNo` | 运单处理事件 | 发货箱、运单号、操作类型、提交账号 |
| `/admin_pda/shipOrderShip/sureShip` | 发货确认事件 | 发货单、箱数、提交账号、交接时间 |

该方案优点是人员、数量和业务状态同成同败，不要求 PDA 额外调“统计接口”，也避免前端成功提示和后端落账不一致。

### 5.2 可选方案：定时同步已有业务日志

如果后端已有完整业务流水，可定时同步到统一台账。同步必须覆盖：

- 新增成功记录。
- 数量、人员、箱号等字段更正。
- 清空、移出、破箱、取消、重新配货、重新装箱等逆向动作。
- 历史回补范围和回补口径。

只按创建时间拉新增记录是不够的，因为统计会漏掉历史更正。

### 5.3 不推荐方案：PDA 前端另发统计报工

不建议在 PDA 成功后再由前端调用一个“人员产量上报接口”。原因：

- 前端二次请求可能失败，导致业务成功但统计缺失。
- 重试和页面退出会制造重复或漏记。
- 可编辑姓名字段容易被误用为人员主键。
- 已有业务接口已经具备动作边界，应让后端在同一事务中固化事实。

## 6. 产品设计优化

### 6.1 PDA 侧

PDA 侧保持现有作业入口，不新增统一报工页。可在必要位置轻量补充：

- 当前登录人或实际处理人展示，避免共用设备时误操作。
- 支持扫员工码/选择实际处理人的场景，必须同时保存提交账号和实际处理人。
- 成功提示仍以业务结果为主，不把“产量已统计”变成现场额外动作。
- 网络未知结果时，提示“正在确认结果”，不要鼓励重复提交。

### 6.2 PC 后台统一人员作业台账

建议 PC 建一个统一入口，分为“明细台账、人员汇总、异常数据”三个视图。

明细筛选：

- 作业日期、同步日期。
- 员工、来源账号。
- 作业类型、对象类型、单位。
- 发货单、箱号、包裹号、商品 ID、库位。
- 来源系统、来源接口。
- 有效、已冲销、待匹配、待核实。

明细字段：

| 字段 | 展示建议 |
|---|---|
| 作业时间 | 按实际作业时间展示，另显示同步时间 |
| 员工 | 统一员工姓名 + 员工编号；可展开来源账号 |
| 作业类型 | 入库、配货、打包、运单、发货等 |
| 当前业务对象 | 发货单、箱、包裹、商品、库位 |
| 本次数量 | 数字 + 单位 |
| 来源 | PDA/小程序/PC + 来源动作 |
| 状态 | 有效、冲销、更正、待核实 |
| 下钻 | 跳转到原业务单据或来源记录 |

### 6.3 人员产量汇总

基础维度固定为：

```text
员工 × 实际作业日期 × 作业类型 × 计量单位
```

展示示例：

```text
张三 2026-09-11
附加项：贴纸 80 作业件次、吊牌 30 作业件次
入库：商品 120 件、包裹 18 个、箱 6 个
配货：商品 96 件、包裹 8 个
打包：装箱商品 88 件、完成发货箱 12 箱
运单：填写 12 箱
发货：确认 5 单
```

不得展示为“张三总产量 375”。如业务确需一个经营指标，应另行定义权重、单价或标准工时，不在本方案中默认折算。

### 6.4 异常数据视图

建议单独展示以下异常，便于上线初期对账：

- 有来源记录但员工账号无法映射。
- 有姓名文本但没有稳定 `admin_id`。
- 有最终状态但无本次增量数量。
- 包裹/箱对象缺少作业时的明细快照。
- 重复来源记录。
- 冲销/移出记录找不到原记录。
- 同一对象多次完成状态同步导致疑似重复。

## 7. 关键统计规则

### 7.1 有效产量

只有业务接口返回成功、后端事务确认成功，并生成可追溯来源记录的动作，才能进入有效产量。失败请求、前端弹窗、页面会话列表、最终状态快照均不是有效产量来源。

### 7.2 本次数量

统计本次成功完成量，不使用计划量、应配量、应打包量或最终累计量替代。

示例：

- 商品配货本次 `goods_qty = 20`，计 20 件。
- 同一发货单 `distributeDone` 后不再追加整单数量。
- 商品装箱本次 `box_info.quantity = 8`，计 8 件。
- 发货箱 `completeBox` 只表示整单打包完成，除非后端没有更细装箱事件且业务确认以箱为统计单位。

### 7.3 分批和多人

同一商品、包裹或箱可以被多次处理。能拆数量的按实际数量拆分；不能拆数量的按对象记录参与人，但不要给每个参与人各计一个完整对象后再相加。

### 7.4 一键配货

`distributeAll` 是本方案重点风险。若它只是系统快捷操作，不能把整单全部待配商品归给点击人。建议处理：

1. 默认单列为“一键配货操作 1 次”。
2. 只有业务确认点击人确实完成了对应实物配货，且后端能展开每个明细的实际数量，才展开计入商品件数。
3. 如果该功能用于补状态或异常处理，应从人员产量中排除，仅保留审计记录。

### 7.5 更正、撤销与返工

| 场景 | 统计处理 |
|---|---|
| 数量录错 | 生成更正或冲销记录，汇总按最新有效数量 |
| 商品从箱中移出 | 冲减原装箱记录或记录逆向作业，不能保留原正向产量不变 |
| 清空箱子 | 冲销该箱内未发货装箱记录，保留操作审计 |
| 破箱/重新装箱 | 破箱单列操作；重新装箱按新动作记录，是否计返工由业务确认 |
| 配货后客户取消 | 已发生配货可保留；后续退回、上架另记对应作业 |
| 清除运单号 | 作为更正，不算正向运单产量 |

### 7.6 作业日期

优先使用实际业务动作发生时间。补传或同步延迟时，汇总归属实际作业日，同时展示同步时间。若历史数据只有提交时间，没有实际作业时间，则页面应明确称为“提交日期”。

### 7.7 清算独立

清算继续使用原业务规则。自动清算、默认完成和状态流转不产生人工产量。人员台账不反向改变清算数量、费用或订单状态。

## 8. 实施分期

| 阶段 | 工作内容 | 完成条件 |
|---|---|---|
| 0. 后端字段核查 | 按接口确认是否已有日志 id、人员 id、本次数量、更正状态 | 形成接口字段映射表和数据缺口清单 |
| 1. 事件落账改造 | 在 `putInStock`、`distribute`、`boxDone` 等成功事务内写作业事件 | 新动作可稳定生成去重作业流水 |
| 2. 员工映射 | 建立 PDA、附加项小程序、PC 账号到员工编号的映射 | 至少试点人员全部可映射，历史未知可待匹配 |
| 3. PC 台账和汇总 | 建明细台账、人员汇总、来源下钻、异常数据 | 可按员工/日期/类型/单位查询并下钻 |
| 4. 试点对账 | 选择一组人员、部分入库/配货/打包动作跑实物对账 | PDA 成功记录、后台来源记录、统计汇总一致 |
| 5. 扩展接入 | 接入运单、发货、清算辅助统计和历史回补 | 有更正规则和回补责任人后再上线 |
| 6. 后续专项 | 代购订单全链路来源追溯、工资绩效、标准工时 | 另立需求，不混入本期验收 |

首批建议优先接入：

- 附加项有效报工记录。
- 商品级扫码入库 `putInStock`。
- 非一键的商品配货 `distribute`。
- 发货装箱 `boxDone`。
- 发货箱完成/运单/发货确认作为辅助统计，待口径确认后接入。

## 9. 验收场景

| 场景 | 预期结果 |
|---|---|
| PDA 商品入库 8 件 | 生成 1 条 `STOCK_IN_GOODS`，员工、商品、库位、数量 8 可追溯 |
| PDA 包裹入库 1 个 | 生成 1 条 `STOCK_IN_PACKAGE`；若无包裹明细快照，不显示商品件数 |
| PDA 箱入库 1 箱 | 生成 1 条 `STOCK_IN_BOX`；箱内件数仅作辅助且有快照才展示 |
| 甲配货 60 件、乙配货 40 件 | 两人分别归属 60、40；发货单合计 100 |
| 同一发货单点击配货完成 | 不再追加 100 件 |
| 点击一键配货 | 默认只统计一键操作，不按全部商品件数归属，除非评审确认 |
| 商品装入发货箱 5 件 | 生成 `SHIP_PACK_GOODS` 5 件 |
| 同一箱填写箱规完成 | 可统计箱规/完成箱数，但不重复追加 5 件商品 |
| 清空箱子 | 原装箱有效汇总被冲销或转为无效，保留清空操作审计 |
| 修改 `pack_name` 为他人姓名 | 展示姓名快照可变，但正式归属仍取稳定实际处理人/员工编号 |
| PDA 和小程序同一员工不同账号 | 员工映射后归并同一员工，来源账号保留 |
| 历史账号无法匹配 | 标记待匹配，不猜测归属 |
| 前端重复点击或网络重试 | 来源唯一键只计一次 |
| 业务失败但前端有临时列表 | 不产生有效产量 |
| 运单填写后又清除 | 填写为正向操作，清除为更正，不重复计正向产量 |
| 确认发货 | 按确认发货单数或箱数统计；不替代配货/打包产量 |
| 自动清算完成 | 不产生人工产量 |
| 延迟同步昨日作业 | 归属实际作业日，显示同步时间 |

## 10. 评审待确认事项

| 编号 | 待确认问题 | 建议参与方 | 优先级 |
|---|---|---|---|
| Q01 | 后端各接口是否已有可用于统计的成功业务流水和唯一记录 id？ | PDA 后端、产品 | P0 |
| Q02 | 后端是否在入库、配货、装箱、发货动作中固化提交账号 admin id？ | PDA 后端 | P0 |
| Q03 | `pack_name`、`distribution_name` 是否只是展示文本，还是已有对应员工 id？ | PDA 后端、仓库主管 | P0 |
| Q04 | 是否允许代操作或扫工牌切换实际处理人？若允许，哪些环节允许？ | 仓库主管、产品 | P0 |
| Q05 | 商品、包裹、箱三类对象的主统计单位如何展示？是否需要箱内/包内件数快照？ | 仓库主管、产品 | P0 |
| Q06 | `distributeAll` 是否计入人员件数产量？如果计入，依据是什么？ | 仓库主管、产品 | P0 |
| Q07 | `distributeDone`、`completeBox`、`sureShip` 是否只算状态，还是也算人工操作次数？ | 产品、运营 | P1 |
| Q08 | 清空、移出、破箱、清除运单如何冲销或标记返工？ | PDA 后端、仓库主管 | P0 |
| Q09 | 历史数据回补从哪一天开始，历史人员如何映射？ | 运营、研发 | P1 |
| Q10 | 统一台账是否需要从来源记录跳转回 PDA/PC 原单据？ | 产品、PC 后台研发 | P1 |
| Q11 | 清算、人工建单是否纳入首批统计，还是只作为后续辅助统计？ | 业务负责人、产品 | P1 |
| Q12 | 是否需要工资、绩效、计件单价或标准工时？ | 业务负责人 | 后续 |

建议评审先完成 Q01-Q08。只要这些问题明确，PC 台账和产量汇总就能按统一事实表推进；Q09 以后可作为上线范围和历史数据策略再拆。

## 11. 研发核查清单

### 11.1 入库

- `/admin_pda/kuStockInLog/putInStock` 成功后是否生成入库日志 id。
- 商品入库的 `quantity` 是否为本次增量，是否允许部分入库、多次入库。
- 包裹/箱入库时是否能记录包含的商品明细快照。
- 入库失败、重复入库、撤销入库如何记录。
- 是否保存提交账号、实际入库人、库位、客户 `user_main_uuid`。

### 11.2 库位箱装箱

- `/admin_pda/kuBoxInfo/inbox` 是否保存每次入箱明细。
- `order_detail_qty` 是否本次入箱数量。
- 包裹入箱时是否保存包裹 id 和包裹内商品快照。
- `outBox`、`clearBox`、`deleteBox`、`updateBox` 是否有操作日志和原记录关系。

### 11.3 配货

- `/admin_pda/shipOrderDistribute/distribute` 是否按商品/包裹/箱分别生成明细。
- `goods_qty` 是否本次增量，是否支持多人分批。
- `distributeAll` 是否展开生成明细，还是只更新最终状态。
- `distributeDone` 是否有提交账号和完成时间；是否只作状态节点。
- 取消发货、重新配货、退回上架如何保存。

### 11.4 发货打包

- `/admin_pda/shipOrderPack/boxDone` 是否每次保存箱内变动明细。
- `box_info.quantity` 是否本次装入数量，而非箱内累计量。
- `is_done=0` 和 `is_done=200` 的业务含义差异。
- `pack_name`、`distribution_name` 是否有对应员工 id；如果没有，是否能补字段。
- `clearBox`、`removeQuantity`、`shipBoxUnbox` 是否能冲销原装箱产量。
- `completeBox` 是否只表示整单打包完成。

### 11.5 运单和发货

- `/admin_pda/shipOrderShip/updateWaybillNo` 是否区分填写、修改、清除。
- 批量填写同一个运单号时按箱数还是操作次数统计。
- `/admin_pda/shipOrderShip/sureShip` 是否代表人工实物交接，还是仅业务状态流转。
- 发货确认失败、取消发货、重新发货如何记录。

## 12. 与 V1.0 的差异摘要

| V1.0 内容 | V1.1 调整 |
|---|---|
| 多处写“尚未核实 PDA 能力” | 已根据前端代码确认 PDA 入口和接口；后端落库字段仍待核实 |
| 配货人员追踪能力直接复用 | 改为可复用 PDA 动作，但人员统计必须要求后端稳定账号/员工字段 |
| 入库、配货、打包作为较粗环节 | 细分商品、包裹、箱、库位箱装箱、发货装箱、完成状态 |
| 打包以包裹或箱计量为主 | 补充 `boxDone` 的商品装箱件数与发货箱完成箱数两套口径 |
| 未具体列 PDA 接口 | 增加来源接口、参数和研发核查清单 |
| 更正规则偏原则化 | 结合 PDA 的清空、移出、破箱、清除运单等动作补充冲销规则 |
| 实施分期偏业务 | 调整为“后端字段核查 → 事件落账 → 员工映射 → PC 台账 → 试点对账” |

## 13. 代码依据

本版主要参考以下 `global-wms` 文件：

- `pages.json`：PDA 页面路由和 tab 入口。
- `api/common/login.js`、`stores/modules/user.js`、`utils/request.js`：登录、用户信息和 token 请求拦截。
- `api/guide/waitStockList.js`：待入库列表接口。
- `api/guide/inboundGuide.js`、`pages/guide/scanInbound/index.vue`：扫码入库接口和提交参数。
- `api/guide/kuBoxInfo.js`、`pages/guide/boxOperation/index.vue`：库位箱装箱、移出、入库相关动作。
- `api/packShip/detail.js`、`pages/packShip/detail/index.vue`：配货、配货完成、打包完成、发货确认相关接口。
- `api/packShip/singlePacking.js`、`pages/packShip/detail/singlePacking/index.vue`：发货装箱和箱内商品数量。
- `api/packShip/shipList.js`、`pages/packShip/detail/shipList/index.vue`、`pages/packShip/detail/shipList/packing/index.vue`：发货箱列表、箱规填写、清空、破箱、移出数量。
- `pages/packShip/deliveryDetail/index.vue`：运单填写、清除和确认发货。
- `components/packShip/FillBox.vue`：`distribution_name`、`pack_name` 为可编辑输入项。

以上依据用于优化产品方案和核查清单。正式开发前仍需以后端接口、数据库字段和生产数据样本为准。
