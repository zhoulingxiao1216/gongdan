# 原型验证记录

日期：2026-09-10。

方法：在 JavaScript V8 环境执行 index.html 的内嵌脚本，使用 verify.js 提供的最小 DOM 与定时器替身。数量函数、数据更新、提交状态和页面 HTML 生成分支共 37 项通过。

这不是浏览器端到端测试：替身不计算布局，不执行真实文件上传，也不能确认实际 DOM、CSS、触屏体验和浏览器兼容性。受控浏览器工具返回“没有可用浏览器”，未获得截图。原型中的“运行数量规则自检”可在用户浏览器中再次执行 10 项基础规则检查。

## 检查结果

- 通过 · 正品7，已报5，剩余2
- 通过 · 允许补报2
- 通过 · 拦截补报3
- 通过 · 报齐后不可重复提交
- 通过 · 正品+2后，仅开放2
- 通过 · 零、负数、小数、NaN均拦截
- 通过 · 未确认人员不可提交
- 通过 · 正品减少冲突阻断
- 通过 · 拍照凭证必填
- 通过 · 有凭证允许拍照报工
- 通过 · 连续两次提交只新增一笔
- 通过 · 正品增加后完整增量提交链路
- 通过 · 增量不覆盖原始5件记录
- 通过 · 失败保留草稿且不写记录
- 通过 · 失败重试只写一次
- 通过 · 上游版本变更先要求复核
- 通过 · 版本复核后可提交
- 通过 · 并发后超出剩余拒绝入账
- 通过 · 并发重新核对后仅提交剩余
- 通过 · 预览返回保留已填数量和人员
- 通过 · 贴纸预览页面可渲染
- 通过 · 吊牌预览页面可渲染
- 通过 · 洗标预览页面可渲染
- 通过 · FBA预览页面可渲染
- 通过 · 预览错误态可渲染
- 通过 · 预览未生成态可渲染
- 通过 · 无附加项商品匹配后可渲染
- 通过 · 无正品商品匹配后可渲染
- 通过 · 不存在商品保持扫码页
- 通过 · 取消退出保留填写
- 通过 · 确认放弃只清除草稿
- 通过 · home页面渲染无异常
- 通过 · scan页面渲染无异常
- 通过 · goods页面渲染无异常
- 通过 · records页面渲染无异常
- 通过 · reserved页面渲染无异常
- 通过 · 报工详情页面渲染无异常

## 仍需实际浏览器验收

- 在 375px、390px、430px 窄屏查看滚动、输入法与底部按钮遮挡。
- 从列表进入四种预览并返回，检查滚动位置与表单保留。
- 实际选择 JPG/PNG/WebP，检查错误大小/格式、图片预览和移除。
- 刷新后检查当前浏览器 localStorage 的保留情况。
- 微信小程序扫码、预览真实文件、后端幂等与并发事务另行接入验收。

