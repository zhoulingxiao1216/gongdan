# 附加项报工小程序 PRD

**版本：V1.1 业务定位修订稿（沿用原文件路径）｜日期：2026-09-10｜产品暂名：质控报工｜平台：微信小程序**

本版整合五份原始文档，并纳入业务方已明确的五项首版要求。用途为需求评审、开发拆解与测试验收。本文是评审稿，不代表所有设计建议已经业务确认，也不代表对应功能已在生产系统实现。

## 最新确认：清算与人员报工独立（V1.1）

业务方明确：现行系统负责清算，到货后默认检品完成、正品后默认已选附加项完成。新增报工用于统计质控人员做了什么附加项、各做了多少，不替换现有清算完成逻辑。

- 检品方式下四个选项按到货数计算；其他附加项（含内侧检品、开封作业）按正品数计算。到货10、正品8，对应数量分别为10和8；不是四种检品方式都必选。
- 本期仍聚焦附加项报工，检品方式的人员报工待后续质检工作台设计，不能因名称含“检品”而误分类。
- 清算默认完成不产生员工产出；报工成功只增加有效报工记录及人员产量，不回写清算数量、金额或完成状态。
- 清算已完成与报工未记录可并存；未报工不证明未做，不新增清算、尾检、入库或发货拦截。
- 正品仍是附加项可报上限。报工侧使用“可报总量／累计已报／剩余未报”，状态为“未报工／部分报工／已报齐”。
- PC与小程序共用同一报工记录和累计；独立的是清算与报工的业务口径，不是两端各自记账。
- 增加人员产量基础汇总为本期目标：实际处理人×附加项×日期的报工数量，可下钻流水；工资、绩效排名及成本仍不在本期。

本节及以下修订优先于五份早期来源和未修订原型中的“待处理、部分完成、待尾检复核”等表述。原型尚需后续按此口径同步，本次不修改HTML。

## 0. 文档说明

### 0.1 文档来源

| 编号 | 来源 | 主要采纳内容 |
|---|---|---|
| S1 | [商品ID二维码报工与QCVAS数字化初版PRD-20260905](商品ID二维码报工与QCVAS数字化初版PRD-20260905.md) | 产品背景、角色职责、商品识别、报工规则、异常、统计与验收 |
| S2 | [建议A-商品ID二维码报工第一版初稿-20260905](建议A-商品ID二维码报工第一版初稿-20260905.md) | 仓内实物流转边界、商品 ID 策略、数量模型、分期与追溯 |
| S3 | [PDA四工作台交互规约-20260905](PDA四工作台交互规约-20260905.md) | 扫码优先、任务操作、数量防错、状态反馈 |
| S4 | [PDA四工作台原型设计说明与页面布局规约-20260905](PDA四工作台原型设计说明与页面布局规约-20260905.md) | 页面布局、字段、数量控件、预览与凭证入口 |
| S5 | [质检与附加服务系统评估改善方案-20260905](质检与附加服务系统评估改善方案-20260905.md) | 已有设计/打印能力的复用线索、实体完工边界、幂等与长期扩展 |
| U | 本次业务方补充确认 | 首版范围、动态正品、数量计量、四类预览、不划分岗位权限 |

引用按章节定位，Markdown 文件无固定页码。S5 中对现有代码的描述是原评估报告的结论，本轮未重新审计其所引用的业务仓库；具体接口和复用条件需要开发核实。

### 0.2 已确认决策

| 决策编号 | 业务方已确认的内容 | 对本版的影响 |
|---|---|---|
| U1 | 首版以附加项报工为主，其他工作台预留入口 | 初检、质检、尾检不展开作业流程 |
| U2 | 附加项只对正品处理；正品数可更新，更新后支持增量报工 | 各项目以最新正品为上限；历史累计保留；已报齐可重新开放 |
| U3 | 存在按 SKU 和按套的项目，首版暂不区分，先按普通计费的数量口径报工 | 本版统一数量录入，不做 SKU/套换算，不据此修改原收费规则 |
| U4 | 贴纸、吊牌、洗标、FBA 均支持预览 | 四类预览为本版核心功能 |
| U5 | 权限暂不划分 | 不设置初检/质检/尾检之间的岗位操作权限差异 |
| U6 | 检品方式按到货数、其他附加项按正品数 | 分组决定基数；内侧检品、开封作业归附加项 |
| U7 | 现行默认完成用于清算；新增报工用于工作人员工作量与产量 | 两种口径独立，不自动计员工产出，不以未报工新增流转拦截 |

### 0.3 需求依据与优先级

本文采用四种依据标记：

- **[确认]**：业务方 U1–U7 已明确。
- **[文件]**：五份原始文档明确提出，未被本轮确认覆盖。
- **[假设]**：为使流程完整而采用的合理设计口径，须在评审中核对。
- **[建议]**：新增设计建议，不自动视为已批准。

优先级与依据分开管理：**P0 为本次核心交付，P1 为本次可选增强，后续为不纳入本次验收的规划。** P0 中涉及假设/建议的细则仍须评审，不因标为 P0 就变成已确认事实。

同一事项以 U 优先；其次采用与本版范围一致的文件要求。原型用于说明交互，不反向决定业务规则。后续若批准本 PRD，冲突处按本文执行；原始文件保留供追溯。

## 1. 产品背景与目标

### 1.1 背景

当前业务是跨境代购/代采履约。订单明细拥有商品 ID，包裹到仓后经过签收、初检、质检、附加项处理、尾检、入库。贴纸、吊牌、剪标、换包装、拍照等作业存在纸质勾选、手写工号和口头交接，系统难以即时掌握实际处理人、完成数量及剩余工作。

已有设计完成、打印完成等状态只能说明设计或打印动作发生，不能证明附加项已经在实物上处理完成。[文件：S1§1、§7；S2§2、§4；S5§3.4]

### 1.2 产品定位

以订单明细商品 ID 为入口，提供面向一线人员的附加项数量报工能力。人员扫描商品后，查看订单已配置项目和最新可处理正品数，按项目记录本次实际完成量，并追溯每笔记录。

本版保留现有现场岗位分工和纸质流转，不建设完整 QC/VAS 生产调度系统，不以报工代替检品、设计、打印或尾检。[确认：U1；文件：S1§2、§4；S2§0]

### 1.3 本版目标

| 目标 | 产品结果 | 观察口径 |
|---|---|---|
| 少查单、少手写 | 扫商品直接进入附加项列表，常用数量一键填充 | 从识别商品到提交的操作步骤和耗时 |
| 记录实际产出 | 每笔记录包含商品、项目、实际处理人、本次数量、时间 | 已成功报工记录的字段完整率 |
| 防止超报和重复计数 | 单项累计不超过最新正品；同一次请求不重复记账 | 数量拦截、重复请求处理结果 |
| 支持增量续报 | 正品增加后自动重新计算各项剩余 | 已报齐项目重新开放与增量记录正确性 |
| 提供标签核对依据 | 四类设计附加项可预览 | 预览文件匹配正确性、成功/失败反馈 |
| 可追溯 | 能查看本次与历史报工 | 记录与商品项目累计数量一致性 |

效率改善比例、时长目标和试点业务基线尚未提供，本 PRD 不编造达标数字。数据正确性按第 11 章场景逐项验收。

## 2. 用户、系统边界与术语

### 2.1 用户和职责

| 对象 | 本版职责 | 边界 |
|---|---|---|
| 附加项实际处理人 | 核对商品、执行实体作业、确认本次数量、提交报工 | 可由初检、质检、尾检人员兼任，不按岗位限制 |
| 当前操作账号 | 进入小程序并提交操作 | 不划分岗位权限仍须能追溯操作身份；不表示匿名报工 |
| 上游业务系统 | 提供商品、订单选项、正品数量及其更新 | 本小程序不编辑正品数、不办理退换货 |
| 设计/打印系统 | 提供贴纸、吊牌、洗标、FBA 对应的预览内容 | 本版不编辑设计、不执行标签打印 |
| 初检、质检、尾检 | 按现有流程履行岗位职责 | 本版仅预留入口，不提供虚假的到货确认或尾检放行 |

实际处理人必填来自文件；默认登录人以及是否允许选择其他员工为身份方案假设，见 Q02。[文件：S1§10.5.5、S3§5.3；确认：U5]

### 2.2 核心术语

| 术语 | 定义 |
|---|---|
| 商品 ID | 订单明细级商品识别码，不是跨订单通用 SKU，也不是单件序列号 |
| 快递单号 | 包裹识别号；签收包裹不等于确认商品到货或正品 |
| 附加项 | 订单为该商品选择的作业项目，如贴纸、吊牌、剪标等 |
| 附加项任务 | 当前商品下一个具体、可区分的已配置附加项；显示名称相同也不应误合并不同配置实例 |
| 当前正品数 G | 上游确认的、当前可用于附加项处理的正品数量，已排除不良和待定 |
| 应做数量 T | 当前项目按业务口径应完成的数量；首版暂按 G 计算，见假设 A01 |
| 累计已报 D | 当前商品当前项目所有有效成功记录的数量合计 |
| 剩余可报 R | 当前项目在最新基数下还允许报工的数量 |
| 本次完成 q | 本次实际完成并提交的新增数量，不是累计总数 |
| 数量已报齐 | 当前项目累计已报等于当前应做，不代表尾检通过或永久完成 |
| 作业件次 | 不同附加项的报工量相加。例如贴纸 7 + 吊牌 7 = 14 作业件次，不等于 14 件商品 |
| 预览 | 查看当前商品对应标签文件内容，不代表打印、贴标或扫码核验已完成 |

## 3. 本期范围与功能清单

### 3.1 本期核心 P0

| 需求编号 | 功能 | 交付要求 | 依据 |
|---|---|---|---|
| FR-01 | 工作台入口 | 附加项作为主入口，初检/质检/尾检预留 | [确认] U1 |
| FR-02 | 商品识别 | 扫商品 ID / VAS 码，支持手动输入与识别失败反馈 | [文件] S1§10.1、§12；S4§4 |
| FR-03 | 商品与任务展示 | 商品摘要、已配置附加项、各项应做/累计/剩余 | [文件] S1§11.3；S3§5 |
| FR-04 | 四类预览 | 贴纸、吊牌、洗标、FBA 查看、放大、返回及异常反馈 | [确认] U4；[假设] 具体交互 |
| FR-05 | 单项报工 | 逐项录入本次数量、确认实际处理人、提交结果 | [文件] S1§10.5、§11.3；S3§5 |
| FR-06 | 动态数量与增量 | 最新正品控制上限，累计保留，正品增加后续报增量 | [确认] U2 |
| FR-07 | 提交一致性 | 失败重试不重复，数量变化时复查，记录与累计一致 | [文件] S5§6；[建议] 具体冲突交互 |
| FR-08 | 报工追溯及人员产量 | 列表与详情查看商品、项目、数量、人员、时间；PC按日期/人员/项目汇总并下钻 | [文件] S1§14.1–14.2；S2§12、§14.2 |

### 3.2 本期可选增强 P1

| 编号 | 能力 | 边界 |
|---|---|---|
| EN-01 | 报工图片凭证 | 沿用项目配置的凭证要求；具体必填项目、张数、格式需确认 |
| EN-02 | 记录筛选/轻汇总 | 商品筛选、笔数、作业件次；不建设完整管理看板 |
| EN-03 | 提示音/震动 | 在目标设备允许时增强反馈，始终保留可见文字提示；不作为提交正确性的依赖 |

现有 HTML 包含这些能力中的部分演示，并不自动使其成为已确认 P0。**如果正式首批试点包含强制图片交付的拍照项目，EN-01 必须随该项目上线，或先将该项目排除试点；不得把上传必需凭证拆到后续而允许无凭证完工。**

### 3.3 本期不包含

完整初检、质检、尾检流程；包裹签收；新增流转单生成/补打；新建生产工单的领取、派工、计时；退换货处置；正品手动更正；返工/冲销；SKU/套差异计量；设计编辑/打印；新旧码双扫码校验；入库/打包/出库新增统一拦截；离线补同步；员工工资、耗材、成本与客户收费重算；完整绩效排名和管理报表。

已有业务系统中的拦截和数据流程不因本项目被取消。本版仅不新增上述流程。[文件：S5§6；确认：U1、U3]

## 4. 业务流程

### 4.1 全链路位置

```text
现有流程：包裹签收 → 初检 → 质检 → 确认可处理正品
                                      ↓
本期范围：进入附加项 → 扫商品 → 看任务/预览 → 实体作业 → 本次数量报工
                                      ↑                     ↓
现有上游：退换货等处置完成 → 正品数量更新                  报工记录/剩余更新
                                                            ↓
现有后续：                                           尾检 → 入库
```

上游处置动作与“可处理正品增加”不是同一件事。只有上游已经确认并发布新的可处理正品数量，附加项才采用新基数；退换货申请本身不得直接扩大报工上限。

### 4.2 主流程

1. 操作人进入小程序，打开“附加项报工”。
2. 扫描商品流转单商品码或手动输入商品 ID。
3. 系统取得商品信息、最新正品、订单已选附加项、各项累计已报和预览引用。
4. 人员核对商品名称、图片、规格及任务指令；必要时打开四类标签预览。
5. 人员完成当前附加项实体作业，进入单项报工页。
6. 输入本次新增完成数量，或点击“完成当前剩余”；确认实际处理人。按配置补齐凭证。
7. 系统校验最新数量和必要字段；通过后提交，处理中锁定本次操作。
8. 成功后产生一笔记录，并更新该项目累计与剩余。
9. 人员选择“继续当前商品”“下一商品”或“查看本次记录”。

### 4.3 增量续报流程

```text
正品 7，贴纸已报 7，剩余 0
  → 上游确认新增 2 件正品，最新正品 9
  → 历史贴纸累计仍为 7
  → 当前贴纸剩余变为 2，重新开放报工
  → 本次填写 2，新增一笔 +2
  → 累计变为 9，剩余 0；原来的记录不改写
```

如果更新前项目还未报齐，新剩余为“原剩余 + 新增正品”，不是仅允许报新增的那部分。例如 G 从 7 增至 9、D 为 5，则 R 为 4；本次仍记录实际新增完成量，不填写累计 9。

### 4.4 取消与失败分支

- 取消扫码：留在扫码页，不产生记录。
- 识别失败：允许重新扫码或手输，不进入错误商品。
- 返回/切商品时有未提交内容：提示是否放弃；继续填写保留草稿，确认放弃只清除未提交数据。[假设]
- 提交被业务拒绝：展示具体原因，保留可用填写内容，不增加累计。
- 提交结果未知：显示“正在确认提交结果”；先查询该次请求，再按同一请求重试，不允许新建同内容请求重复计数。[建议，依据 S5§6]
- 无附加项：提示无需报工，可下一商品；不能把“已有项目全部报齐”显示成“无需附加项”。

## 5. 核心业务规则

### BR-01 商品与项目识别

报工绑定订单明细商品 ID 和具体附加项任务，不按项目名称或跨订单 SKU 合并累计。只允许对订单当前有效配置的项目报工；未选择项目不得临时新增。[文件：S1§6、§10.5.3；建议：任务实例识别]

快递单号不直接用来提交附加项报工，快递签收不自动增加正品。二维码只是识别入口，不承载权威数量，扫码后读取系统最新数据。[文件：S1§8.2；S2§5]

### BR-02 计量口径

本版统一按普通数量报工。界面统一显示“件”，数量为正整数。[确认：U3；假设：单位与整数限制]

原有按 SKU、按套计费的配置及客户收费快照不改写；本项目先统一执行数量录入，不推导客户应收、员工工资或成本。不将上传图片张数直接等同拍照报工数量。[确认：U3；建议：计费与执行分离，依据 S5§6]

### BR-03 应做与剩余

对于商品下每一个有效项目 i：

```text
G = 上游最新可处理正品数
Tᵢ = G                              （首版假设 A01）
Dᵢ = 该商品该项目有效成功记录数量合计
Rᵢ = max(0, Tᵢ − Dᵢ)

本次 q 必须满足：q 为正整数，q ≤ Rᵢ，Dᵢ + q ≤ G
```

**A01：[假设] 所有已配置项目覆盖该商品全部当前正品。** 业务方确认的是“正品上限”，没有明确所有项目是否必定作用于全部正品。若存在独立应做量，需改为项目应做量与正品上限共同约束，不能用本假设覆盖订单指定数量。上线前需核对 Q01。

各项目独立比较上限，不将多个项目累计相加后与 G 比较。G=7 时贴纸报 7、吊牌报 7 均合法；同一贴纸项目累计 8 不合法。

### BR-04 本次新增与累计

成功后：Dᵢ 更新为 Dᵢ+q，新增一条数量为 q 的记录。不允许把本次输入当成“累计覆盖值”；历史记录不因正品更新而改变。[确认：U2；文件：S1§10.5.4]

G=7、D=5 时，本次可以报 1 或 2；报 1 后剩余 1，可由另一人员补报。一个项目可多人分次处理，但每笔记录都须有实际处理人。

### BR-05 动态正品

进入商品页、主动刷新、提交前必须取得或校验最新 G 和 D。重新进入已报齐项目不能仅依据旧“完成”状态永久禁用。[确认：U2；建议：刷新时机]

当 G 增加时，重新计算所有已配置项目的剩余；D 不变，新增正品本身不产生报工记录、不增加人员产出。增量完成后新增记录，保留当时的正品数量快照。[确认：U2；建议：快照]

### BR-06 正品减少与净数量不变的替换

这两类场景未获业务明确确认，按建议处理并列为风险：

| 场景 | 本稿建议 | 限制 |
|---|---|---|
| G 减少但所有 D≤G | 更新剩余，保留历史，提交时重新核对 | 不自动假定减少的商品已做或未做哪项 |
| G 减少且任一 D>G | 标记数量待核对，暂停该商品报工，联系负责人 | 不删记录、不自动生成负数，不提供绕过按钮 |
| 退一换一，G 净值不变 | 不自动开放新报工 | 实物需要重做时须单独返工口径，见 Q06 |
| G 曾减少又恢复 | 不将净恢复量无条件当成可重做量 | 必须排查是否涉及替换件、重复计算或有效累计纠正 |

本版增量模型解决“最新正品增加后的未报剩余”，不提供单件追踪，不能据此识别一件实物是否重复贴标。[建议]

### BR-07 处理人

实际处理人必填，提交前须确认。[文件：S3§5.3、S4§4.4]

本稿默认带出登录员工；是否可切换其他员工、是否扫工牌待 Q02 决定。若支持代操作，记录提交账号与实际处理人，不能仅保存可随意填写的姓名文本。[假设/建议]

“暂不划分权限”指本版不区分岗位操作权，不等于取消身份记录，也不等于授权更改上游正品、作废历史或操作全量外部业务数据。[确认：U5；假设：身份基础]

### BR-08 四类预览与完工边界

贴纸、吊牌、洗标、FBA 必须能从对应任务进入预览；匹配当前商品及该项目的正式预览内容，不显示其他商品文件。[确认：U4]

预览、打印、实体处理、报工、尾检复核是不同动作。预览成功不增加 D，也不自动将任务改为报齐；本版不把看过预览等同双扫码验证。[文件：S5§3.4；假设：非强制预览]

文件缺失或加载失败必须明确提示，不能显示占位内容却声明为正式标签。是否缺失时禁止报工，由 Q03 决定；评审默认可返回，未额外添加“预览必通过”的提交门槛。

### BR-09 数量状态与复核状态分开

| 数量状态 | 条件 | 可报工 |
|---|---|---|
| 无附加项 | 无有效已配置任务 | 否，无需报工 |
| 暂无正品 | G=0，未发生累计冲突 | 否，等待上游 |
| 未报工 | G>0 且 D=0 | 是 |
| 部分报工 | 0<D<T | 是 |
| 数量已报齐 | D=T 且 G>0 | 否；G 增加后可重新打开 |
| 数量待核对 | D>G 或上游数据无法确认 | 否，处理异常 |

按 U7，本版不根据报工新建“待尾检复核”状态，也不改变原尾检流程。报工状态仅表示人员记录完整程度，不能作为清算或实物是否完成的重新判定。

### BR-10 并发、重复与结果未知

同一次成功提交只生成一笔有效记录。客户端连点、扫码重复进入、断网重试都不得重复计数。[文件：S5§6]

系统须在写入时对最新数量进行不可分割的校验和更新。两个员工同时争抢剩余 2 件，各报 2 件时，最多一笔成功，另一笔返回最新剩余与失败原因。[建议：一致性要求]

表单显示数据已变更时，本稿建议先刷新并要求人员核对，再提交合法 q；不能默默把其填写的 2 改成 1 后入账。

网络超时不直接认定失败。若请求已成功则返回原记录；确认未成功才能重试。未知结果不应展示“报工成功”，也不应让人员误以为可重新新建一笔。

### BR-11 历史与上线初始化

历史纸质勾选、设计完成、打印完成不得直接作为实体已报工数。[文件：S5§3.4；建议：初始化]

首批上线前需确定哪些商品从零开始，哪些有经过核实的历史完成量。尚无记录的历史商品只能显示未报或历史未归属，不能显示实物未做。初始化应有来源和责任人标识，不伪造成上线后员工的新报工产出。具体方式见 Q07。

### BR-12 订单项目变更

读取不到配置与“配置为空”必须区分，接口错误不能显示“无需附加项”。未识别的已选服务应提示配置待核对，不静默忽略。[建议]

已有报工后取消、替换项目或改变标签内容，不能自动删除流水或沿用旧完工判定。此类数据应保留并提示核对；项目变更、返工和有效累计调整的完整规则不在本版，需通过试点范围控制。[建议]

## 6. 功能详细需求

### FR-01 工作台入口

**目标**：人员进入后快速开始附加项作业。

- 附加项报工为首要入口，显示当前身份。
- 初检、质检、尾检展示“预留入口”，点击后说明尚未开放，可返回；不能显示权限不足。
- 提供报工记录入口；不需要复杂首页看板。
- 验收：所有岗位人员均可进入附加项流程；预留入口不能提交业务数据。

### FR-02 商品扫码与手输

**前置**：上游可按商品 ID 提供数据；员工已有可用身份。

- 支持识别商品 ID 码和业务定义的 VAS 商品码。[文件]
- 二维码编码协议复用现有规则，由开发核实；`VAS/DDEMO001` 仅是原型示例，不强行作为正式唯一格式。
- 识别成功进入商品页；输入为空、码类型错误、商品不存在、网络异常分别反馈。
- 取消扫码不改变当前记录；手输可作为摄像头不可用时的备用入口。
- 商品级数据不存在或获取失败时不允许用空值代替正品数后报工。

### FR-03 商品摘要与任务列表

**展示内容**：商品 ID、图片、商品名称、颜色/尺码/规格、必要订单信息、当前正品、任务指令、各项应做/累计/剩余。

- 仅展示已选任务；多个同名配置实例应能区分，不依名称合并。
- 尚未报齐项目优先；已报齐可折叠或置灰，仍能查看历史和预览。[文件：S4§4.3]
- 未知配置、暂无正品、无附加项、全部报齐是独立状态。
- “刷新数量”取得最新数据；“下一商品”回到扫描入口。
- 商品图片加载失败用明确的“图片未加载”说明，文字信息保留；不使用不相关商品图片。

### FR-04 四类预览

| 类型 | 必须可查看的内容 | 数据来源原则 |
|---|---|---|
| 贴纸 | 该商品对应的贴纸预览 | 关联到具体商品及贴纸配置 |
| 吊牌 | 该商品对应的吊牌预览 | 不与贴纸或其他商品混用 |
| 洗标 | 该商品对应的洗标预览 | 展示真实已提供文件，不由小程序生成成分内容 |
| FBA | 该商品对应的 FBA 标签预览 | 展示真实文件，不在本版新建编码规则 |

- 列表及当前报工页均提供入口。[假设]
- 支持查看、放大/缩小或适合手机的文件阅读方式；具体图片/PDF格式待 Q03。
- 返回原入口，保留未提交的数量、人员与凭证。
- 已报齐后仍允许预览；不提供设计编辑、打印、修改标签内容。
- 加载中、加载失败、尚未生成、版本失效要区分，并提供重试或联系处理指引。

### FR-05 单项报工

**只处理一个当前项目，不增加批量勾选多项目一次提交。** [文件：S3§1、§5；假设：首版交互拆分]

| 字段 | 必填 | 默认/来源 | 校验/行为 |
|---|---|---|---|
| 商品与当前项目 | 是，系统带出 | 已识别商品与已选任务 | 不在表单随意改项目身份 |
| 当前正品、累计、剩余 | 是，只读 | 最新系统数据 | 不让员工修改 |
| 本次完成数量 | 是 | 初始空或 0，不默认自动提交 | 正整数，不能超过最新剩余 |
| 实际处理人 | 是 | 暂定登录人 | 确认后才能提交；是否允许切换待 Q02 |
| 图片凭证 | 按项目配置 | 本次选择 | 采用 EN-01 时按正式约束验证 |
| 操作账号/提交时间 | 是，系统记录 | 当前身份/系统时间 | 不允许手改历史事实 |

按钮：“−”“+”“完成当前剩余”“查看预览”（四类）“取消”“提交报工”。输入有误展示具体原因并禁用提交；发送中禁用重复提交和修改。

成功页展示项目、本次 +q、人员、时间、记录号、最新累计和剩余；提供继续当前商品、下一商品、查看本次记录。不新增待尾检复核状态，不改变清算或入库流程。

### FR-06 动态数量与增量续报

- 项目“数量已报齐”必须由最新数量关系计算，不作为不可逆终态。
- 正品更新后，页面明确提示基数发生变化，人员无需删除或重开旧报工。
- 多项目分别重新计算；先前已报齐的项目可能重新开放，未报齐项目保留原剩余并加入新增基数。
- 保留原记录，并能够在新记录中说明其对应的提交时正品基数。
- 退换货按钮、正品手工加减等评审模拟控件不出现在正式小程序。

### FR-07 提交结果与重试

- 成功、校验拒绝、确定未成功、结果待确认分别反馈。
- 记录写入和累计更新必须一起成功，不能出现“流水有记录但累计未变”或相反结果。
- 同次操作的重复请求应能查询同一结果；不把重试统计为新作业。
- 数量冲突时展示最新正品/已报/剩余，保留用户输入供修改；不自动截断其本次报量。
- 确定失败时允许重试；未知结果先确认，不承诺离线成功。

### FR-08 报工记录、详情与人员产量

- 按提交时间展示有效成功记录，至少可在当前商品下查看各笔作业。
- 每条展示：商品 ID、项目、本次数量、实际处理人、提交时间、记录号。
- 详情展示上述字段及当时数量快照，已接入凭证时可查看。
- 首版只读，不提供删除、更正、冲销；这些操作留待独立规则。
- PC提供人员产量查询：日期、实际处理人、附加项、商品筛选；按人员×项目汇总有效报工数量，可下钻明细。[确认：U7；建议：查询布局]
- 不将报工记录列表命名为“工资”或“收费明细”。
- 筛选无结果显示空状态，不把接口错误显示为“暂无记录”。

### EN-01 凭证增强

文件提出按项目配置决定必填，尚未给出统一张数/格式。正式要求应来自已确认的项目配置，不照搬原型“单张 2 MB”的演示限制。

采用时至少支持上传、预览、移除和失败重试；凭证关联具体本次记录。上传未完成不视为证据已提交；强制凭证项目需上传成功后才能报工。拍照服务产出数量按本版作业数量口径，不按附件个数自动增加。

## 7. 页面与交互规格

### 7.1 页面清单

| 页面 | 目标与内容 | 主要操作/跳转 |
|---|---|---|
| P01 工作台 | 当前人员、附加项主入口、其他三岗预留、记录入口 | 报工→P02；记录→P07；预留→P09 |
| P02 扫描商品 | 扫码/手输与查询反馈 | 成功→P03；返回→P01 |
| P03 商品与附加项 | 商品信息、当前正品、独立项目进度 | 报工→P04；预览→P05；记录→P07；下一商品→P02 |
| P04 填写报工 | 当前单项的数量、处理人、凭证 | 提交成功→P06；预览→P05；取消→P03 |
| P05 预览 | 四类共用预览能力、关联商品、加载状态 | 放大/重试；返回原 P03/P04 |
| P06 成功结果 | 本次新增量与最新剩余 | 当前商品→P03；下一商品→P02；详情→P08 |
| P07 记录列表 | 各笔成功记录，商品筛选为可选增强 | 详情→P08；工作台→P01 |
| P08 记录详情 | 本次报工事实与凭证 | 返回→P07 |
| P09 预留工作台 | 初检/质检/尾检尚未开放说明 | 返回→P01 |

一级页面可采用“工作台 / 报工记录”双 tab；作业子页隐藏 tab，底部固定主次操作。[假设] 微信实际导航实现方式由开发适配，不改变上述返回关系。

### 7.2 通用布局

沿用 S3§1–2、S4§1 的“顶部导航和状态—中部内容—底部操作”。关键数量不低于 28px，商品 ID/重要状态不低于 18px，普通内容以 16px 为目标；主操作建议 56–64px，次操作不少于 44px。字号为设计标尺，实装需适配小程序缩放与设备。

不依赖颜色表达唯一含义：绿色配“可报工”，黄色配“部分报工”，红色配具体拦截原因，灰色配“数量已报齐/尚未开放”。避免复杂表格、密集小字和长文本输入。

一屏只填一项报工。返回、切商品、预览后返回等行为保持可预测；校验错误靠近相关字段，成功页不能替代必要的状态更新。

### 7.3 提示文案

| 场景 | 建议文案 |
|---|---|
| 扫错码 | 请扫描商品 ID 或 VAS 码。 |
| 未找到商品 | 未找到商品，请检查商品 ID 后重新查询。 |
| 配置读取失败 | 附加项加载失败，请重试。 |
| 无项目 | 该商品无需附加项报工。 |
| 暂无正品 | 暂无可处理正品，请等待数量更新。 |
| 非法数量 | 请输入大于 0 的整数。 |
| 超量 | 本项最多还能报 {R} 件，请修改本次数量。 |
| 人员未确认 | 请确认本次实际处理人。 |
| 数量变更 | 数量已更新，请核对最新剩余数后再次提交。 |
| 提交中 | 正在提交，请稍候。 |
| 结果未知 | 正在确认提交结果，请勿重复报工。 |
| 明确失败 | 提交未成功，填写内容已保留，请重试。 |
| 成功 | 报工成功，本次完成 {q} 件。 |
| 预览缺失 | 预览文件尚未生成，请联系负责设计或打印的人员。 |
| 预览失败 | 预览加载失败，请重试。 |
| 数量冲突 | 正品数小于累计报工数，请联系负责人核对。 |
| 放弃填写 | 放弃本次填写？已成功报工的记录会保留。 |

## 8. 数据与外部依赖

本章定义业务数据职责，不规定表名、接口路径、数据库结构或技术选型。S5 中的 SQL、接口和代码位置保留为开发参考，不直接复制为本版产品需求。

### 8.1 业务对象与必要数据

| 对象 | 必要数据 | 来源/写入方 |
|---|---|---|
| 商品摘要 | 商品 ID、订单关联、名称、图片、规格 | 上游读取 |
| 正品快照 | 当前可处理正品数、数据更新时间或等效版本信息 | 上游发布，本版只读 |
| 任务配置 | 具体任务标识、项目名称、指令、是否有效、预览/凭证配置 | 订单已选附加项读取 |
| 任务汇总 | 当前应做、累计已报、剩余、数量状态 | 由有效记录与最新基数计算 |
| 预览引用 | 商品/项目关联、文件引用、可用状态、版本信息 | 已有设计/打印系统提供 |
| 报工记录 | 记录号、商品、任务、本次数量、单位、实际处理人、提交账号、时间 | 成功提交时创建 |
| 追溯补充 | 提交时正品基数、凭证引用、同次操作识别信息 | [建议] 记录保存 |

### 8.2 外部能力依赖

1. **商品解析**：确认二维码实际内容及其与订单明细的映射，不能依赖原型的演示编号规则。
2. **正品权威来源**：明确哪个系统提供“可处理正品”，更新事件或刷新机制由开发设计；不能直接使用包裹签收、应采购量或总到货量替代。
3. **附加项配置**：核实普通、设计、自定义项目如何对应可报工任务；未知项目需核对，不能漏做。
4. **四类预览**：核实 S5 描述的现有设计/打印文件是否可供小程序读取、是否对应最新正式内容。
5. **人员身份**：复用现有员工身份能力，正式入口与登录方式由开发核实；不额外建设岗位权限体系。
6. **记录写入**：需要统一的报工事实来源，避免小程序写入与原后台重复计数。

### 8.3 统计口径

成功记录笔数：有效成功报工记录数量；失败、重复请求、数量更新事件不计入。

作业件次：有效成功记录 q 合计，可按商品/项目/处理人汇总；不同项目相加需标注“作业件次”。商品件数不得直接用该合计替代。

人员产出归属实际处理人，不默认归属代操作账号。历史初始化单独标识，不算作上线后新报工。新增正品只改变任务基数，不产生员工产出。

本期增加PC端按日期、实际处理人、附加项的基础报工量查询与汇总，可下钻有效记录。工资规则、成本归集、收费变化和排名不在本期。默认按提交日期统计时必须称“当日报工量”；如需补录作业，实际作业日期与提交时间分别记录的方案须确认，不能混称实际日产量。

## 9. 异常与非功能要求

### 9.1 异常处理责任

| 异常 | 系统行为 | 后续处理对象 |
|---|---|---|
| 错误码/无商品 | 不进入报工；重扫/手输 | 当前操作人 |
| 数据加载失败 | 保留错误态，禁用依赖该数据的提交 | 重试；持续失败交技术支持 |
| 无项目 | 明确无需报工 | 下一商品 |
| G=0 | 不可报工 | 等待上游确认 |
| 超报/未确认人员 | 拦截且保留填写 | 当前操作人修改 |
| 并发或正品变化 | 刷新并提示复核 | 当前操作人 |
| 提交未知 | 查询原请求，不盲目新建 | 系统确认；持续异常交支持 |
| 预览缺失/失效 | 说明问题，不假冒成功文件 | 设计/打印负责人 |
| 正品减少冲突 | 暂停报工、保留历史 | 业务负责人核对 |
| 项目配置变更/无法识别 | 提示核对，不静默跳过 | 配置维护/业务负责人 |
| 实物需要返工但净数量不变 | 不突破正常数量上限 | 后续返工规则，不能用重复正常报工解决 |

责任对象是建议的协同角色，不自动新增系统权限或通知功能；具体支持入口与人员在试点前明确。

### 9.2 非功能要求

- **正确性**：任何客户端绕过或并发请求都不能突破有效数量上限；前端校验不能代替最终写入校验。
- **一致性**：报工记录与累计同成同败，重复请求返回同一结果。
- **可恢复性**：明确失败保留表单；未知结果可追查；不提供假离线成功。
- **可读性**：重点覆盖小屏、较大系统字体与弱光仓库场景，按钮不被软键盘或底部安全区遮挡。
- **可追溯性**：能关联商品、任务、实际人、提交人、本次数量、时间；基数变化不覆盖历史。
- **身份与数据边界**：不因“不分岗位权限”而取消身份校验或改变已有系统的数据归属边界；图片展示遵循现有员工使用范围。
- **兼容性**：目标微信版本、设备型号、扫码方式、文件格式和可接受响应时长由开发/现场确认，不在本稿虚构环境指标。
- **反馈**：关键操作有可见加载与结果；声音/震动作为可选增强，不要求用户依赖听觉完成判断。

## 10. 原文件冲突与整合结论

| 原文件差异/未定项 | 本版处理 | 依据/状态 |
|---|---|---|
| 原 P0 包含初检、质检、尾检、看板；现聚焦附加项 | 本期只做附加项核心，其他入口预留 | [确认] U1 |
| 原“已完成置灰”与“确认是否追加”并存 | 无剩余禁止重复；正品增加后按最新剩余开放 | [确认] U2 优先；S1§10.5 |
| 拍照有“应做 1”、按件、按张等不同示例 | 本版统一普通数量，不以示例 1 张作为全局规则 | [确认] U3；具体单位假设待核对 |
| 洗标/FBA 未在部分工作台列表中列出 | 只要配置有效就纳入；四类均有预览 | [确认] U4 |
| 新旧码双扫码在 S5 列为 P0 | 本版先交付预览，不额外引入双扫码强制门槛 | [假设] 按 U1/U4 收敛，未来单独评审 |
| 现场岗位权限、工牌与主管更正方案混杂 | 本版无岗位权限差异；身份必录，人员切换待确认；不做主管更正 | [确认] U5；[假设] Q02 |
| 待定未清禁止尾检 vs 正品可部分放行 | 保留冲突，本期不实现尾检；附加项仅采用有效正品 | [确认] U1/U2；后续尾检评审 |
| 到货即打票 vs 初检确认后打印 | 打票不属于本版；扫码依赖现有商品码入口 | [确认] U1；后续确认 |
| 照片“建议上传”与“未上传拦截”并存 | 按项目正式配置判断，不一刀切；拍照上线需对应凭证规则 | [文件] S4§4.4；Q04 |
| S5 包含 SQL/API/成本/全链路拦截 | 抽取业务一致性原则，技术方案另评；远期功能不进入本次承诺 | 本版文档边界 |

## 11. 验收标准

以下 P0 验收以已确认规则及评审通过的假设为准。每项失败需有可观察反馈和数据核对，不能只验证按钮可点击。

| 用例 | 前置与操作 | 预期结果 | 对应需求 |
|---|---|---|---|
| AC-01 | 初检/质检/尾检人员进入 | 均可进入附加项；其他三岗仅预留 | FR-01/U5 |
| AC-02 | 扫有效商品 ID 或 VAS 码 | 进入正确订单明细的任务列表 | FR-02/BR-01 |
| AC-03 | 扫错码、无商品、查询失败 | 明确错误且不可报工，可重试/手输 | FR-02 |
| AC-04 | 有效商品无配置项目 | 显示无需报工，下一商品可用 | FR-03 |
| AC-05 | 正品为 0 | 已配置任务不可提交报工 | FR-03/BR-03 |
| AC-06 | 同商品正品 7、贴纸累计 5 | 应做 7、累计 5、剩余 2；一键剩余填 2 | FR-03/05 |
| AC-07 | 上述条件分别报 1、再报 1 | 两笔 +1，累计 7、剩余 0 | FR-05/BR-04 |
| AC-08 | 剩余 2，输入 3/0/负数/小数 | 全部拦截，不增加记录或累计 | FR-05/BR-03 |
| AC-09 | 正品 7，贴纸 7、吊牌 7 | 两项目均合法，合计 14 标为作业件次 | BR-03/§8.3 |
| AC-10 | 贴纸累计 7、正品 7，再尝试报 1 | 拦截，不因“追加”突破上限 | FR-06 |
| AC-11 | 正品 7→9、贴纸累计 7 | 剩余变 2，重新开放；新增正品不自动计产出 | FR-06 |
| AC-12 | 正品 7→9、贴纸累计 5 | 剩余变 4；本次最多 4 | FR-06 |
| AC-13 | 在 AC-11 后报 2 | 新增 +2 记录，累计 9；原历史不覆盖 | FR-06/08 |
| AC-14 | 两人同时对剩余 2 各提交 2 | 最多一笔成功；另一笔冲突，不超报 | FR-07 |
| AC-15 | 连点、同请求重试、成功响应丢失 | 仅一笔有效记录，原请求可查到同一结果 | FR-07 |
| AC-16 | 明确提交失败后重试 | 内容保留；失败不计数，最终成功只计一次 | FR-07 |
| AC-17 | 未选择/未确认实际处理人 | 不允许提交，显示提示 | FR-05/BR-07 |
| AC-18 | 分别预览贴纸/吊牌/洗标/FBA | 文件与当前商品/项目匹配，可查看并返回；D 不变 | FR-04 |
| AC-19 | 填表后进入预览再返回 | 数量、人员和已选凭证保留 | FR-04/05 |
| AC-20 | 预览失败/未生成 | 不展示错误商品或假文件，反馈可重试/联系 | FR-04 |
| AC-21 | 填表后取消，分别继续/放弃 | 继续保留；放弃只清未提交内容 | FR-05 |
| AC-22 | 从成功页查看列表和详情 | 商品、项目、q、人员、时间一致，可追溯历史 | FR-08 |
| AC-23 | 正品减少至低于某项累计 | 按批准的 BR-06 处理，不删历史或自动负数报工 | FR-06，待建议评审 |
| AC-24 | 配置请求失败/存在未知项目 | 不误显示无附加项，不漏项报齐 | FR-03/BR-12 |
| AC-25 | 已有打印或清算默认完成状态但无人员报工 | 不自动计员工产出；保持原清算状态 | BR-08/11、U7 |
| AC-26 | 清算附加项数量8；甲报贴纸5、乙报贴纸3 | 两人产出分别5、3；报工累计8；清算数量/金额不变 | U7/FR-08 |
| AC-27 | 清算默认完成，人员报工为0 | 报工显示未报，不据此新增尾检/入库/清算拦截 | U7 |
| AC-28 | 甲贴纸5、开封8 | 分项分别5、8；合计标13作业件次，不标13件独立商品 | FR-08 |

P1 凭证采用时补充验收：必填未上传/上传中不可提交；上传失败可重试；成功后凭证关联正确记录；附件数量不自动改变报工件数。

## 12. 试点与交付建议

### 12.1 建议阶段

| 阶段 | 产出/完成条件 |
|---|---|
| 需求评审 | 确认本版范围，处理 Q01–Q04 与试点依赖，明确建议规则是否采用 |
| 数据对接 | 核实商品、正品、项目、身份、预览和历史累计来源 |
| 核心开发 | 完成 FR-01–FR-08；采用的 P1 随对应服务一起交付 |
| 联调验收 | 执行 AC 用例，包含并发、超时与正品变化；实机测试 |
| 小范围试点 | 选择一组人员和明确的服务清单，核对线上记录与现场事实 |
| 扩展 | 试点稳定后再评审其他三岗与返工、计量差异 |

人员安排、工期与上线日期未提供，本表不构成排期承诺。

### 12.2 首次上线前必须明确

- 使用哪些商品范围和附加项类型，是否包括凭证必需的拍照服务。
- 哪个接口或数据源代表最新可处理正品。
- 四类真实预览是否齐备、历史记录如何初始化。
- 哪些事项需要负责人处理，以及联系入口。
- 对包含净数量不变替换、已报后改项目等未覆盖场景如何暂时隔离。
- 既有设计/打印/尾检/入库流程继续运行，不因小程序上线被误判为已替代。

### 12.3 原型与正式功能的差异

[现有 HTML 原型](prototype/index.html) 可用于页面走查；[原型评审说明](prototype/需求评审说明.md) 和 [验证记录](prototype/验证记录.md) 提供交互参考。

原型为模拟扫码、模拟标签、浏览器本地记录；右侧数量加减、并发、失败开关均为评审工具。正式实现不得交付这些员工可操作的模拟控件。原型无真实后端事务、摄像头扫码、生产标签或跨设备同步，不能以原型自检替代正式验收。

## 13. 后续规划：保留五份文档中的完整业务方向

本章仅保留原文档中的业务需求，**不纳入 V1.0 的开发与验收范围，也不代表已排期**。

| 方向 | 核心能力 | 仍需确认的边界 |
|---|---|---|
| 初检工作台 | 扫包裹/商品；选择多商品；确认到货与待定；异常原因、工单关联、流转单打印 | 超收如何批准；打印时机；待定处置 |
| 质检工作台 | 按客户检品方式处理；只检可检数量；正品+不良=本次检品；记录不良发现人与事件 | 照片强制条件；事件与工单职责 |
| 尾检工作台 | 展示前序事实；复核实物、隔离、标签与包装；未报/未完成/不一致时退回 | 部分正品放行、待定阻断范围、纠错权限 |
| 工票与物理流转 | 商品二维码、重打原因、实体分流、必要工位指令 | 与现有单据关系、是否新增工单领取 |
| 防错增强 | 贴标新旧码校验、统一入库/打包/发货拦截、声音与工位设备适配 | 启用服务范围、比较字段、兼容旧状态 |
| 返工与更正 | 退换货同量替换、重新贴标、报工纠错、有效累计与审计 | 返工是否计产出、冲销与原记录关系 |
| 差异计量 | 数量、SKU、套、张等单位按项目配置 | 换算、应做量、历史单位冻结 |
| 管理与经营 | 员工/小组产出、时效、计件、耗材、服务成本、订单毛利 | 绩效单价、客户收费、权限及财务口径 |

## 14. 假设、建议与待确认清单

### 14.1 设计假设汇总

| 编号 | 假设 | 若不成立的影响 |
|---|---|---|
| A01 | 已配置项目应做量等于全部当前正品 | 需要项目独立应做数量，修改剩余公式与显示 |
| A02 | 本版统一用正整数“件”录入 | 有小数或其他单位时需调整控件和校验 |
| A03 | 处理人默认登录人 | 需适配共用账号、代操作或工牌身份 |
| A04 | 预览是辅助核对，未强制预览通过 | 需增加预览准备状态与提交门槛 |
| A05 | 本次不新增领取/开始/结束计时 | 若需计工时或锁定任务，应单独扩展 |

### 14.2 待确认事项

| 编号 | 问题 | 当前建议/处理 | 确认阶段 |
|---|---|---|---|
| Q01 | 是否每项都作用于全部正品，是否存在独立应做量？ | 暂按 A01；上线前核对实际服务配置 | 核心数量开发前 |
| Q02 | 实际处理人仅限登录员工，还是支持代操作/工牌切换？ | 默认登录人，保留真实人和提交账号的区分 | 身份方案确认前 |
| Q03 | 四类预览的文件来源、格式、版本，以及无文件是否拦截报工？ | 复用已有正式文件；非强制门槛为暂定 | 预览联调前 |
| Q04 | 首批是否上线拍照服务，凭证需哪些角度/张数/格式？ | 按项目配置；有强制凭证则随任务一起交付 | 试点服务清单确认前 |
| Q05 | 正品减少导致已报超出时，由谁、通过何种流程核对？ | 本版暂停并保留历史，暂不提供更正按钮 | 涉及该场景上线前 |
| Q06 | 等量退换货需重做附加项，如何定义返工与有效累计？ | 不突破正常报工上限；先作为独立返工规划 | 覆盖该场景前 |
| Q07 | 上线前已处理商品的历史累计如何核实与初始化？ | 不由打印状态推导，不伪造新产出 | 上线前 |
| Q08 | 首批支持哪些实际设备、微信环境及响应目标？ | 由现场/开发确定真实验收矩阵 | 联调验收前 |

以上不是对 U1–U5 的重复询问；已确认范围继续有效。新建议中，数量减少暂停、数量快照、并发复核、未知结果确认与历史初始化的处理方式均应在评审中明确采纳与否。

## 15. 来源追溯与版本记录

### 15.1 来源—本版需求映射

| 来源章节 | 原始要点 | 本版落点 |
|---|---|---|
| S1§1–7；S2§0–4 | 现场业务与纸面数据痛点 | §1–2 背景、目标、边界 |
| S1§10.1、§11.3；S2§5 | 商品 ID/VAS 码作业入口 | FR-02、BR-01 |
| S1§10.5、§13；S2§7 | 正品、分次报工、数量限制 | BR-02–05；U2/U3 覆盖静态口径 |
| S3§5；S4§4 | 任务列表、单项数量、人员、凭证、按钮状态 | FR-03/05、EN-01、§7 |
| S5§0、§3.4；U4 | 设计类附加项与实体完工区别、四类预览 | FR-04、BR-08 |
| S5§6 | 幂等、兼容既有配置 | FR-07、BR-10–12 |
| S1§14；S2§12、§14 | 人员产出与报工流水 | FR-08、§8.3；经营看板列后续 |
| S3§3/4/6；S4§2/3/5 | 初检、质检、尾检工作台 | U1 下仅保留入口，详细能力列§13 |
| S5§4.3、§5 | 技术对象、全链路拦截、成本路线 | 业务依赖列§8，技术设计另行，扩展列§13 |
| U1–U5 | 首版业务决策 | §0.2、§3、BR-02–09，作为优先依据 |

### 15.2 版本记录

| 版本 | 日期 | 变更 |
|---|---|---|
| V1.1 业务定位修订稿 | 本轮补充确认 | 按U6/U7明确清算与报工独立，纠正数量基数分组和状态语义，新增人员产量查询及验收；原文件路径保留 |
| V1.0 整合评审稿 | 2026-09-10 | 合并五份来源，按 U1–U5 收敛首版；新增动态正品与增量规则；明确四类预览；剥离远期和技术方案；补齐异常、一致性、依赖、验收与待确认事项 |

本版输出是独立 PRD，五份来源文档和现有原型保持原样。评审后建议在本文件更新决策与变更记录，避免多份需求继续产生不一致口径。
